syntax = "proto3";

package gaapicommon.enums;
option csharp_namespace = "GAAPICommon.Enums";

enum ReleaseFlag {
    Unspecified = 0;
    Alpha = 1; // Internal test
    Beta = 2; // External test
    ReleaseCandidate = 3; // For the testing team
    Release = 4; // Official release
}
